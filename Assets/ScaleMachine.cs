using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GunScale : MonoBehaviour
{
    /*
    public bool scaleType; //true больше false меньше

    private void OnCollisionEnter(Collision other)
    {
        float lSX = other.transform.localScale.x;
        if (other.gameObject.CompareTag("Gun"))
        {
            if(scaleType == false)
            {
                other.transform.localScale = new Vector3(lSX - 0.5f, lSX - 0.5f, lSX - 0.5f);
            }
            else
            {
                other.transform.localScale = new Vector3(lSX + 0.5f, lSX + 0.5f, lSX + 0.5f);
            }
        }
    }
    */
}
